{
  "name": "sagittarius",
  "version": "1.0.0",
  "description": "typescript project template with testing, and docker",
  "main": "index.js",
  "scripts": {
    "inspect": "nodemon --watch ./dist --inspect-brk=0.0.0.0:9229 --nolazy ./dist/index.js", 
    "test": "mocha -r ts-node/register src/**/*.spec.ts",
    "start": "nodemon --watch ./dist ./dist/index.js",
    "debug": "npm run prestart && concurrently \"npm run watch\" \"npm run inspect\"",
    "watch": "tsc -w -p .",
    "prestart": "tsc -p .",
    "docker:debug": "docker-compose build debug && docker-compose up debug",
    "docker:app" : "docker-compose build app && docker-compose up app"
  },
  "author": "Moses Aronov",
  "license": "MIT",
  "devDependencies": {
    "@types/chai": "^4.1.2",
    "@types/mocha": "^2.2.48",
    "chai": "^4.1.2",
    "concurrently": "^3.5.1",
    "mocha": "^5.0.1",
    "nodemon": "^1.17.1",
    "ts-node": "^5.0.1"
  },
  "dependencies": {
    "typescript": "^2.7.2"
  }
}
